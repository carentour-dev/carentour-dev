import { DEFAULT_JOURNEY_STEPS, JourneyStepsSection } from "@/components/home";

const ProcessSection = () => {
  return <JourneyStepsSection steps={DEFAULT_JOURNEY_STEPS} />;
};

export default ProcessSection;
