"use client";

import { DEFAULT_HOME_CTA_CONTENT, HomeCtaSection } from "@/components/home";

const CTASection = () => {
  return <HomeCtaSection content={DEFAULT_HOME_CTA_CONTENT} />;
};

export default CTASection;
