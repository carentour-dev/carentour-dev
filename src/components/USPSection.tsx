"use client";

import {
  DEFAULT_DIFFERENTIATORS,
  DifferentiatorsSection,
} from "@/components/home";

const USPSection = () => {
  return <DifferentiatorsSection items={DEFAULT_DIFFERENTIATORS} />;
};

export default USPSection;
